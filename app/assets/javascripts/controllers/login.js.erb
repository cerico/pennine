scenic.controller("LoginController", [ '$scope','$location','$http','Auth', function($scope,$location,$http,Auth) {
  $(".header").css("visibility","none")
    $scope.auth = Auth;
    console.log(Auth)
    if ($scope.auth.isLoggedIn){
    	console.log("crider")
    }
  var backgroundnumber = Math.floor(Math.random() * 7)+1;
     $(".intro").css("background", "url('assets/backdrop" + backgroundnumber + ".jpg') no-repeat center center fixed");
console.log(current_user)
}])


// scenic.controller('LoginController',['$scope', 'Auth', function($scope, Auth) { 
//   $scope.auth = Auth;
//   console.log(Auth)
// }])